SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_ANON_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=your_groq_api_key_from_console.groq.com
# Groq is fallback only — free tier is fine, 
# only kicks in if Gemini API is down
REDIS_URL=
META_VERIFY_TOKEN=
META_APP_SECRET=
ENVIRONMENT=development
CORS_ORIGINS=["http://localhost:3000"]
PLATFORM_WA_PHONE_NUMBER_ID=your_platform_phone_number_id
PLATFORM_WA_ACCESS_TOKEN=your_platform_permanent_access_token
# This is YOUR platform's WhatsApp number, not any client's number
# Register a separate WhatsApp Business number for platform notifications
# Get these from Meta Developer Portal → Your App → WhatsApp → API Setup
